pipeline {
    agent { docker { image 'python:3.5.1' } }
    stages {
        stage('Build') {
            steps {
                sh 'echo test'
                //sh 'python --version'
                //sh 'python Driver.py'
            }
        }
    }
}
